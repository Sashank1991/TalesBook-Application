<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cmpe277project.nk.org.a277_socialmobileapp">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!--
 <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

android:icon="@drawable/ic_launcher"
    </application>
    -->
    <application
        android:name=".volley.AppController"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:theme="@style/AppTheme"
        android:label="Talesbook">
        <activity android:name=".Home" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".login.UserActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
            <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        -->
        <activity
            android:name=".posts.NonUserProfile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".login.SwipeActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".login.LoginVerification" />
        <activity android:name=".Messaging.MessagingActivity" />
        <activity android:name=".Messaging.ConversationActivity" />
    </application>

</manifest>